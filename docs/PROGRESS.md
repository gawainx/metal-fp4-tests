# 进度记录

- (b682c46) : 2026.09.25 将比较范围扩展到 BF16、FP8 E4M3 和 FP4 E2M1，每种精度均提供原生 TensorOps 与软件解码矩阵乘法，并以同精度的软件耗时除以原生耗时呈现倍数。三种格式使用可精确表示的相同输入值，计时仅取 GPU 命令执行区间；程序校验 CPU 参考结果，保存 M1 Max 与 M5 Max 的计算耗时记录。发布包包含全部可执行文件和 Metal 库，运行时无需 Xcode。
